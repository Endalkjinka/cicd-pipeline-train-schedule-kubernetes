version: 0.2
 
phases:
  install:
    runtime-versions:
      nodejs: 16
    commands:
      - apt-get install -y unzip

  build:
    commands:
      - echo Build started on `date` 
   
artifacts:
  type: 
  files:
      - "**/*"
  Base-directory:
      - "https://github.com/Endalkjinka/cicd-pipeline-train-schedule-kubernetes.git"

   
